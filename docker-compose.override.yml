services:
    server:
        command: npm run dev
    
    worker:
        command: npm run dev

    nginx:
        volumes:
            - ./nginx/nginx.prod.conf:/etc/nginx/nginx.prod.conf
            - /etc/nginx/ssl:/etc/nginx/ssl:ro
            - ./client/dist:/usr/share/nginx/html

    client-dev:
        container_name: client-dev
        image: node:20-alpine
        working_dir: /app
        command: sh -c "npm install && npm run dev -- --host"
        ports:
            - '5173:5173'
        env_file: ./.env
        volumes:
            - ./client:/app