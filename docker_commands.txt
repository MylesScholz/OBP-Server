docker run -d --name rabbitmq-server -p "5672:5672" -p "15672:15672" rabbitmq:management

docker run -d --name mongo-server -p 27017:27017 -e "MONGO_INITDB_ROOT_USERNAME=root" -e "MONGO_INITDB_ROOT_PASSWORD=Practicalstoragesystem" mongo:latest
docker exec -it mongo-server /bin/bash
mongosh --username root --password Practicalstoragesystem --authenticationDatabase admin
use api-backend
db.createUser({ user: "api-server", pwd: "Practicalstoragesystem", roles: [ { role: "readWrite", db: "api-backend" }] })